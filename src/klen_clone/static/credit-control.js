async function creditControlWorkspace(options={}){
  const asOf=options.asOf||new Date().toISOString().slice(0,10),q=options.q||'',page=Math.max(1,Number(options.page||1));
  const data=await api(`/credit-control?as_of=${asOf}&q=${encodeURIComponent(q)}`),can=p=>currentPermissions.has(p),controls=data.controls||{};
  const post=async(url,payload)=>{const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify(payload)}),body=await response.json().catch(()=>({}));if(!response.ok)throw Error(body.detail||'Credit-control action failed');return body};
  const customers=data.customers||[],pages=Math.max(1,Math.ceil(customers.length/PAGE_SIZE)),current=Math.min(page,pages),visible=customers.slice((current-1)*PAGE_SIZE,current*PAGE_SIZE);
  const customerOptions=customers.map(x=>`<option value="${esc(x.party_code)}">${esc(x.party_name)}</option>`).join('');
  const blockedOptions=(data.blocked_quotations||[]).map(x=>`<option value="${esc(x.quotation_key)}">${esc(x.quotation_no)} — ${esc(x.party_name)} — ${money(x.order_amount)} — ${esc(x.block_reason.replaceAll('_',' '))}</option>`).join('');
  const dunningOptions=(data.dunning_candidates||[]).map(x=>`<option value="${esc(x.party_code)}">${esc(x.party_name)} — ${money(x.overdue_amount)} overdue — ${num(x.broken_promises)} broken promises</option>`).join('');

  const limitForm=can('credit.limit.prepare')?`<section class="panel draft-panel"><div class="panel-head"><h3>Request customer credit limit</h3><span>Independent approval required</span></div><form id="credit-limit-form"><div class="draft-fields"><label>Customer<select name="party_code" required><option value="">Select customer</option>${customerOptions}</select></label><label>Credit limit AED<input name="proposed_limit" type="number" min="0" step="0.01" required></label><label>Payment terms days<input name="proposed_terms_days" type="number" min="0" max="3650" value="30" required></label></div><label class="notes">Reason<textarea name="reason" minlength="5" maxlength="2000" required></textarea></label><div class="draft-actions"><button class="primary" type="submit">Request approval</button></div><p id="credit-limit-result" class="footer-note"></p></form></section>`:'';
  const overrideForm=can('credit.override.prepare')?`<section class="panel draft-panel"><div class="panel-head"><h3>Request blocked-order override</h3><span>One quotation · one expiry · one use</span></div>${blockedOptions?`<form id="credit-override-form"><div class="draft-fields"><label>Blocked quotation<select name="quotation_key" required><option value="">Select blocked quotation</option>${blockedOptions}</select></label><label>Valid until<input name="valid_until" type="date" value="${asOf}" min="${asOf}" required></label></div><label class="notes">Business justification<textarea name="reason" minlength="5" maxlength="2000" required></textarea></label><div class="draft-actions"><button class="primary" type="submit">Request override approval</button></div><p id="credit-override-result" class="footer-note"></p></form>`:'<p class="empty">No accepted quotation is currently blocked by an approved credit profile.</p>'}</section>`:'';
  const collectionForm=can('collection.manage')?`<section class="panel draft-panel"><div class="panel-head"><h3>Record collection follow-up</h3><span>Promise and next-action controls</span></div><form id="collection-form"><div class="draft-fields"><label>Customer<select name="party_code" required><option value="">Select customer</option>${customerOptions}</select></label><label>Invoice (optional)<input name="invoice_no" maxlength="80" placeholder="Target ERP invoice"></label><label>Action date<input name="action_date" type="date" value="${new Date().toISOString().slice(0,10)}" required></label><label>Action type<select name="action_type"><option value="call">Call</option><option value="email">Email</option><option value="visit">Visit</option><option value="promise_to_pay">Promise to pay</option><option value="final_notice">Final notice</option><option value="legal_referral">Legal referral</option></select></label><label>Next follow-up<input name="next_followup_date" type="date"></label><label>Promise amount AED<input name="promise_amount" type="number" min="0.01" step="0.01"></label><label>Promise date<input name="promise_date" type="date"></label></div><label class="notes">Outcome / commitment<textarea name="outcome" minlength="3" maxlength="2000" required></textarea></label><div class="draft-actions"><button class="primary" type="submit">Save follow-up</button></div><p id="collection-result" class="footer-note"></p></form></section>`:'';
  const dunningForm=can('collection.escalation.prepare')?`<section class="panel draft-panel"><div class="panel-head"><h3>Request collection escalation</h3><span>Sequential dunning · independent approval</span></div>${dunningOptions?`<form id="dunning-form"><div class="draft-fields"><label>Eligible customer<select name="party_code" required><option value="">Select overdue or broken-promise customer</option>${dunningOptions}</select></label><label>Dunning stage<select name="stage"><option value="reminder_1">Reminder 1</option><option value="reminder_2">Reminder 2</option><option value="final_notice">Final notice</option><option value="legal_referral">Legal referral</option></select></label></div><label class="notes">Evidence and reason<textarea name="reason" minlength="5" maxlength="2000" required></textarea></label><div class="draft-actions"><button class="primary" type="submit">Request escalation approval</button></div><p id="dunning-result" class="footer-note"></p></form>`:'<p class="empty">No customer is eligible: dunning requires a target-ERP overdue invoice or a broken promise.</p>'}</section>`:'';

  const customerRows=visible.map(x=>{const available=x.available_credit===null?'Not configured':money(x.available_credit),hold=can('credit.hold.release')&&x.configured?`<button class="credit-hold" data-party="${esc(x.party_code)}" data-action="${x.profile_status==='on_hold'?'release':'hold'}" data-revision="${x.profile_revision}">${x.profile_status==='on_hold'?'Release hold':'Place hold'}</button>`:'—';return `<tr><td><b>${esc(x.party_name)}</b></td><td class="money">${x.configured?money(x.credit_limit):'Not configured'}<small>${x.payment_terms_days===null?'':`${num(x.payment_terms_days)} days`}</small></td><td class="money">${money(x.receivable_exposure)}<small>${money(x.open_order_commitment)} open orders</small></td><td class="money">${available}</td><td class="money">${money(x.overdue_amount)}<small>${num(x.overdue_invoices.length)} invoices</small></td><td>${x.next_followup_date?esc(x.next_followup_date):'—'}<small>${num(x.open_promises)} open · ${num(x.broken_promises)} broken</small></td><td><span class="status ${esc(x.credit_status)}">${esc(x.credit_status.replaceAll('_',' '))}</span>${x.hold_reason?`<small>${esc(x.hold_reason)}</small>`:''}</td><td class="row-actions">${hold}</td></tr>`}).join('')||'<tr><td colspan="8" class="empty">No customers match this search.</td></tr>';
  const requestRows=(data.requests||[]).map(x=>{let actions='—';if(x.status==='pending'&&can('credit.limit.approve'))actions=`<button class="credit-request-action primary-small" data-key="${esc(x.request_key)}" data-action="approve" data-revision="${x.revision}">Approve</button><button class="credit-request-action" data-key="${esc(x.request_key)}" data-action="reject" data-revision="${x.revision}">Reject</button>`;else if(x.status==='pending'&&can('credit.limit.prepare'))actions=`<button class="credit-request-action" data-key="${esc(x.request_key)}" data-action="cancel" data-revision="${x.revision}">Cancel</button>`;return `<tr><td>${esc(x.party_name)}</td><td class="money">${money(x.proposed_limit)}</td><td>${num(x.proposed_terms_days)} days</td><td>${esc(x.reason)}</td><td>${esc(x.created_by)}</td><td><span class="status ${esc(x.status)}">${esc(x.status)}</span></td><td class="row-actions">${actions}</td></tr>`}).join('')||'<tr><td colspan="7" class="empty">No credit-limit requests.</td></tr>';
  const overrideRows=(data.override_requests||[]).map(x=>{let actions='—';if(x.status==='pending'&&can('credit.override.approve'))actions=`<button class="override-action primary-small" data-key="${esc(x.request_key)}" data-action="approve" data-revision="${x.revision}">Approve</button><button class="override-action" data-key="${esc(x.request_key)}" data-action="reject" data-revision="${x.revision}">Reject</button>`;else if(x.status==='pending'&&can('credit.override.prepare'))actions=`<button class="override-action" data-key="${esc(x.request_key)}" data-action="cancel" data-revision="${x.revision}">Cancel</button>`;return `<tr><td>${esc(x.quotation_no)}<small>${esc(x.party_name)}</small></td><td>${esc(x.block_reason.replaceAll('_',' '))}</td><td class="money">${money(x.exposure_snapshot)} + ${money(x.order_amount)}<small>Projected ${money(x.projected_exposure)} vs limit ${money(x.credit_limit_snapshot)}</small></td><td>${esc(x.valid_until)}</td><td>${esc(x.reason)}</td><td><span class="status ${esc(x.status)}">${esc(x.status)}</span></td><td class="row-actions">${actions}</td></tr>`}).join('')||'<tr><td colspan="7" class="empty">No credit override requests.</td></tr>';
  const actionRows=(data.actions||[]).map(x=>{const promise=x.promise_amount===null?'—':`${money(x.promise_amount)} by ${esc(x.promise_date)}`,buttons=x.status==='open'&&can('collection.manage')?`<button class="collection-action" data-key="${esc(x.action_key)}" data-action="complete" data-revision="${x.revision}">Complete</button><button class="collection-action" data-key="${esc(x.action_key)}" data-action="break" data-revision="${x.revision}">Broken</button><button class="collection-action" data-key="${esc(x.action_key)}" data-action="cancel" data-revision="${x.revision}">Cancel</button>`:'—';return `<tr><td>${esc(x.action_date)}</td><td>${esc(x.party_name)}<small>${esc(x.invoice_no||'General account follow-up')}</small></td><td>${esc(x.action_type.replaceAll('_',' '))}</td><td>${esc(x.outcome)}</td><td>${esc(x.next_followup_date||'—')}</td><td>${promise}</td><td><span class="status ${esc(x.status)}">${esc(x.status)}</span></td><td class="row-actions">${buttons}</td></tr>`}).join('')||'<tr><td colspan="8" class="empty">No collection actions recorded.</td></tr>';
  const dunningRows=(data.dunning_requests||[]).map(x=>{let actions='—';if(x.status==='pending'&&can('collection.escalation.approve'))actions=`<button class="dunning-action primary-small" data-key="${esc(x.request_key)}" data-action="approve" data-revision="${x.revision}">Approve</button><button class="dunning-action" data-key="${esc(x.request_key)}" data-action="reject" data-revision="${x.revision}">Reject</button>`;else if(x.status==='pending'&&can('collection.escalation.prepare'))actions=`<button class="dunning-action" data-key="${esc(x.request_key)}" data-action="cancel" data-revision="${x.revision}">Cancel</button>`;else if(x.status==='approved'&&can('collection.escalation.prepare'))actions=`<button class="dunning-action primary-small" data-key="${esc(x.request_key)}" data-action="complete" data-revision="${x.revision}">Mark issued</button>`;return `<tr><td>${esc(x.party_name)}</td><td>${esc(x.stage.replaceAll('_',' '))}</td><td>${money(x.overdue_snapshot)}<small>${num(x.broken_promises)} broken promises</small></td><td>${esc(x.reason)}</td><td>${esc(x.created_by)}</td><td><span class="status ${esc(x.status)}">${esc(x.status)}</span></td><td class="row-actions">${actions}</td></tr>`}).join('')||'<tr><td colspan="7" class="empty">No dunning requests.</td></tr>';

  content.innerHTML=hero('Credit control & collections','Approved limits, blocked-order overrides, overdue follow-up and sequential dunning',`As at ${data.as_of} · posting disabled`)+`<section class="panel"><form id="credit-search" class="filters"><label>As at<input name="as_of" type="date" value="${esc(asOf)}" required></label><label>Customer search<input name="q" value="${esc(q)}" placeholder="Customer name"></label><button class="primary" type="submit">Refresh controls</button></form></section><div class="metrics control-metrics">${metric('Configured',num(controls.configured_customers),'Approved credit profiles')}${metric('Blocked orders',num(controls.blocked_orders),'Accepted quotations requiring override')}${metric('Pending overrides',num(controls.pending_overrides),'Independent approval')}${metric('On hold',num(controls.on_hold),'Order conversion blocked')}${metric('Overdue',num(controls.overdue_customers),data.overdue_scope)}${metric('Dunning',num(controls.pending_dunning),'Pending escalation approvals')}</div>${limitForm}${overrideForm}${collectionForm}${dunningForm}<section class="panel"><div class="panel-head"><h3>Customer credit exposure</h3><span>Opening control + invoices + open orders − approved receipts</span></div><div class="table-wrap"><table><thead><tr><th>Customer</th><th>Limit / terms</th><th>Exposure</th><th>Available</th><th>Overdue</th><th>Follow-up</th><th>Credit status</th><th>Actions</th></tr></thead><tbody>${customerRows}</tbody></table></div>${pager(customers.length,current)}</section><section class="panel"><div class="panel-head"><h3>Credit-limit approval queue</h3><span>${num(controls.pending_limit_requests)} pending</span></div><div class="table-wrap"><table><thead><tr><th>Customer</th><th>Proposed limit</th><th>Terms</th><th>Reason</th><th>Requested by</th><th>Status</th><th>Actions</th></tr></thead><tbody>${requestRows}</tbody></table></div></section><section class="panel"><div class="panel-head"><h3>Blocked-order override queue</h3><span>${num(controls.pending_overrides)} pending · one-time use</span></div><div class="table-wrap"><table><thead><tr><th>Quotation / customer</th><th>Block</th><th>Exposure + order</th><th>Expires</th><th>Justification</th><th>Status</th><th>Actions</th></tr></thead><tbody>${overrideRows}</tbody></table></div></section><section class="panel"><div class="panel-head"><h3>Collection action history</h3><span>${num(controls.open_collection_actions)} open</span></div><div class="table-wrap"><table><thead><tr><th>Date</th><th>Customer / invoice</th><th>Action</th><th>Outcome</th><th>Next follow-up</th><th>Promise</th><th>Status</th><th>Actions</th></tr></thead><tbody>${actionRows}</tbody></table></div></section><section class="panel"><div class="panel-head"><h3>Dunning escalation queue</h3><span>${num(controls.pending_dunning)} pending</span></div><div class="table-wrap"><table><thead><tr><th>Customer</th><th>Stage</th><th>Eligibility</th><th>Reason</th><th>Requested by</th><th>Status</th><th>Actions</th></tr></thead><tbody>${dunningRows}</tbody></table></div><p class="footer-note">Dunning is sequential: reminder 1 → reminder 2 → final notice → legal referral. Every stage needs independent approval and the previous stage must be completed.</p></section><p class="footer-note">Overdue values use approved target-ERP invoices because migrated BizModo due dates were not reliable. Opening balances remain in exposure but are not falsely aged.</p>`;

  document.querySelector('#credit-search').onsubmit=e=>{e.preventDefault();creditControlWorkspace({asOf:e.target.as_of.value,q:e.target.q.value.trim(),page:1})};
  bindPager(next=>creditControlWorkspace({asOf,q,page:next}));
  const limit=document.querySelector('#credit-limit-form');if(limit)limit.onsubmit=async e=>{e.preventDefault();const out=document.querySelector('#credit-limit-result'),payload=Object.fromEntries(new FormData(e.target));out.textContent='Submitting controlled request…';try{await post('/api/v1/credit-control/limit-requests',payload);await creditControlWorkspace({asOf,q})}catch(error){out.textContent=error.message}};
  const override=document.querySelector('#credit-override-form');if(override)override.onsubmit=async e=>{e.preventDefault();const out=document.querySelector('#credit-override-result');out.textContent='Submitting one-time override request…';try{await post('/api/v1/credit-control/overrides',Object.fromEntries(new FormData(e.target)));await creditControlWorkspace({asOf,q})}catch(error){out.textContent=error.message}};
  const collection=document.querySelector('#collection-form');if(collection)collection.onsubmit=async e=>{e.preventDefault();const out=document.querySelector('#collection-result'),payload=Object.fromEntries(new FormData(e.target));for(const key of ['invoice_no','next_followup_date','promise_amount','promise_date'])if(!payload[key])payload[key]=null;out.textContent='Saving collection action…';try{await post('/api/v1/credit-control/collections',payload);await creditControlWorkspace({asOf,q})}catch(error){out.textContent=error.message}};
  const dunning=document.querySelector('#dunning-form');if(dunning)dunning.onsubmit=async e=>{e.preventDefault();const out=document.querySelector('#dunning-result');out.textContent='Submitting escalation request…';try{await post('/api/v1/credit-control/dunning',Object.fromEntries(new FormData(e.target)));await creditControlWorkspace({asOf,q})}catch(error){out.textContent=error.message}};
  document.querySelectorAll('.credit-request-action').forEach(b=>b.onclick=async()=>{const note=await askReason(`Enter the reason to ${b.dataset.action} this credit-limit request:`);if(!note)return;try{await post(`/api/v1/credit-control/limit-requests/${b.dataset.key}/${b.dataset.action}`,{expected_revision:Number(b.dataset.revision),note});await creditControlWorkspace({asOf,q})}catch(error){alert(error.message)}});
  document.querySelectorAll('.override-action').forEach(b=>b.onclick=async()=>{const note=await askReason(`Enter the reason to ${b.dataset.action} this one-time credit override:`);if(!note)return;try{await post(`/api/v1/credit-control/overrides/${b.dataset.key}/${b.dataset.action}`,{expected_revision:Number(b.dataset.revision),note});await creditControlWorkspace({asOf,q})}catch(error){alert(error.message)}});
  document.querySelectorAll('.credit-hold').forEach(b=>b.onclick=async()=>{const note=await askReason(`Enter the reason to ${b.dataset.action} this customer credit profile:`);if(!note)return;try{await post(`/api/v1/credit-control/profiles/${encodeURIComponent(b.dataset.party)}/${b.dataset.action}`,{expected_revision:Number(b.dataset.revision),note});await creditControlWorkspace({asOf,q})}catch(error){alert(error.message)}});
  document.querySelectorAll('.collection-action').forEach(b=>b.onclick=async()=>{try{await post(`/api/v1/credit-control/collections/${b.dataset.key}/${b.dataset.action}`,{expected_revision:Number(b.dataset.revision)});await creditControlWorkspace({asOf,q})}catch(error){alert(error.message)}});
  document.querySelectorAll('.dunning-action').forEach(b=>b.onclick=async()=>{const note=await askReason(`Enter the reason to ${b.dataset.action} this dunning stage:`);if(!note)return;try{await post(`/api/v1/credit-control/dunning/${b.dataset.key}/${b.dataset.action}`,{expected_revision:Number(b.dataset.revision),note});await creditControlWorkspace({asOf,q})}catch(error){alert(error.message)}});
}
